### 服务划分

* `demo-api`
* `probe`
* `alert`
* （可选）`sentinel-ui`

### 关键流程

1. Probe 拉取目标配置（DB/配置文件）
2. 执行 HTTP 探测，采集：状态码/RT/错误摘要
3. 探测事件落库
4. Alert 服务消费事件/定时扫描窗口
5. 去重/聚合/分级后发送通知
6. 告警落库，支持追溯与统计

### 关键取舍

* 为什么用“主动探测”而不是只靠被动监控
* 为什么要做“去重/聚合/静默”
* 为什么事件要落库（可审计、可统计、可复盘）